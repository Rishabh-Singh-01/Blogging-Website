import { protectController } from '../controllers/authController';
import { updateLikeController } from '../controllers/postController';

const express = Require('express');

const router = express.Router();

module.exports = router;
